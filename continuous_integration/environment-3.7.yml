name: mg_igraph

channels:
- conda-forge

dependencies:
# dev environment
  - python=3.7
  - coverage
  - pytest
  - pytest-cov
  - black

# dependencies (so setup.py develop doesn't pip install them)
  - metagraph
  - python-igraph
